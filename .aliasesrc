# Aliases
# Tyler Smith
# April 30th, 2015

# This file is imported into both bash and zsh

[[ -f ~/.aliases/apps ]] && source ~/.aliases/apps
[[ -f ~/.aliases/servers ]] && source ~/.aliases/servers
[[ -f ~/.aliases/shortcuts ]] && source ~/.aliases/shortcuts
